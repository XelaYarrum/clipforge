"""ClipForge's Digital Human service.

Script -> voice -> presenter video -> captions -> vertical render -> the existing
posting queue. Every stage is swappable, every stage prefers the free local route,
and no stage names a vendor above the provider layer.
"""
