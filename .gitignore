config/credentials/*
data/
__pycache__/
.venv/
*.log

# Downloaded model weights. LatentSync alone is 9.6 GB — one `git add -A` away
# from being committed to a public repository by accident.
models/

# Anything the twin generates or is given: voice recordings, presenter footage,
# screen captures, finished videos. All of it lives under data/ already, but this
# is the second lock on the door that matters most.
data/twin/
